# TimeMark 进度快照与交接（2026-09-16）

> 新对话从这份文档接续。项目根：`E:\T-Minus`；计划主文档：`docs/TimeMark-开发计划-v2.md`（M0–M5）。
> **M3 Android 端计划已定稿（2026-09-17）**：`docs/TimeMark-Android端开发计划-M3.md`（M3.0–M3.6 阶段拆分、六条契约对齐、风险对策）。下一步 = M3.0 脚手架（`apps/android`，无需 SDK 即可开工）。

## 一、已完成

### 1. 开发计划 v2（定稿）
- 6 条评审意见全部落实：§3 计时状态规则（四类型状态机/暂停恢复/单调时钟/session）、§4 数据模型（通用同步列 + run_json/config_json schema）、§5 同步协议（change_seq 游标、operation_id 幂等、pull 游标规则、pending_ops 队列）、§6 分层冲突策略（配置 LWW / 运行状态 base_version+session / 墓碑胜出）、§8 里程碑 M0–M5 含验收条件。

### 2. Windows 客户端（`apps/windows`，Electron 37 + React 18 + TS）
- 三种计时完整状态机（日期倒计时/精确倒计时/正计时），严格按 §3：暂停值持久化、重启不推进、单调时钟守卫（系统时间被拨不跳变）、每次开始新 session。
- 深色现代 UI：渐变主题、SVG 环形进度、登录弹窗、同步状态灯、细滚动条；时长支持**天/时/分/秒** + 快捷预设。
- 托盘常驻（关窗隐藏+提示通知）、到点系统通知、D-day 提醒、JSON 全量导出（`TimeMark.bat` 双击启动）。
- 登录 + 同步引擎：pending_ops 持久化队列、push/pull 循环（4s）、孤儿数据认领、冲突丢弃后由 pull 回写；未登录纯本地可用。
- 数据位置：`%APPDATA%/timemark-windows/timemark.db`（sql.js）。
- 已排掉的坑：`ELECTRON_RUN_AS_NODE=1`（WorkBuddy 环境必清）；GPU 崩溃循环（已内置 `--in-process-gpu` 等）；**改 IPC 面必须同批改 preload.ts 并重建**，否则渲染层白屏。

### 3. 同步服务端
- **开发版**：`server/`（Node + Express + sql.js，:8787）——仅本机调试，数据与正式版不互通，可弃用。
- **正式版**：`backend/`（Spring Boot 3.3.4 + spring-jdbc + BCrypt + 手写 HS256 JWT，零多余依赖）。
  - API 与开发版契约一致：health / auth.register|login|me / sync.pull（游标分页）/ sync.push（幂等+冲突+墓碑）。
  - schema.sql 全幂等，首次启动自动建表；配置全走环境变量：`TIMEMARK_DB_URL` / `TIMEMARK_DB_USER` / `TIMEMARK_DB_PASSWORD` / `TIMEMARK_JWT_SECRET`(可选) / `TIMEMARK_SERVER_PORT`。
  - 构建：`mvn -s .mvn-settings.xml -DskipTests package`（项目级阿里云镜像）→ `backend/target/timemark-server.jar`。
  - **测试：16 项契约冒烟（smoke.mjs，H2）全过。**

### 4. 云数据库（已建好，可直接用）
- 腾讯云 TDSQL-C MySQL：`sh-cynosdbmysql-grp-2gf1ewak.sql.tencentcdb.com:28451`，公网可达（本机实测 123ms）。
- 已执行：建库 `timemark`(utf8mb4) + 9 张表（users/timer_item/tag/timer_tag/milestone/timer_record/change_log/ops/meta）+ 3 索引（`tools/db-init.cjs`）。
- 集成验证通过：后端连云库 health/register/push/pull 4/4（`tools/cloud-verify.mjs`），测试数据已清理（users=0, timers=0）。

### 5. 部署目标服务器巡检（118.195.133.25，杨志勇服务器）
- Ubuntu 22.04，4C/3.3G/40G（41%），118 天未重启，可用内存 1.9G——**再跑一个 TimeMark 后端毫无压力**。
- SSH：`ubuntu@118.195.133.25` + 密钥 `E:\A-桌面文件\开发\密钥\杨志勇服务器ssh连接.pem`（ACL 已修复）；本机执行 SSH 用 `-F E:\T-Minus\tools\ssh-config -o UserKnownHostsFile=E:\T-Minus\tools\known_hosts`（避开沙箱拦 ~/.ssh）。
- 在跑的业务：`lamp.service`（药盒后端 jar，434MB，:6060，保留）、sub2api 三件套（Docker，**占 8080**，9/15 部署，保留）、cloudflared 隧道、nginx(80/443)、系统 redis（仅本机）、腾讯云 agent。
- 已清理：4 个停止 4 周的 knowhub 容器（用户确认）。
- 可选清理（未做）：1.87GB 未用镜像 + 566MB build cache（`docker image prune -a` 需谨慎，会删所有未用镜像）。

### 6. 正式后端已部署上线（2026-09-16）
- `timemark-server.jar`（md5 a55263ab16a4247109484c1f0a1b52b7）已上传至 `118.195.133.25:/opt/timemark/`。
- systemd 单元 `/etc/systemd/system/timemark.service`（chmod 640，开机自启，Restart=on-failure）：`User=ubuntu`，环境变量 `TIMEMARK_DB_PASSWORD` + `TIMEMARK_SERVER_PORT=18080`，`-Xms128m -Xmx256m`。
- 服务器已有 OpenJDK 21.0.9（原计划的 JRE 17 不需要装）。
- 验证：服务器本机与公网 `http://118.195.133.25:18080/api/v1/health` 均 200 `{"ok":true}`；安全组 18080 已放行（无需操作）。
- JWT secret 未设环境变量：首次启动自动生成并持久化到 meta 表，重启不失效。
- **客户端接口地址已内置（2026-09-16）**：`syncClient.ts` 的 `DEFAULT_SERVER` 与渲染层 `SYNC_SERVER` 常量均硬编码为 `http://118.195.133.25:18080`；登录弹窗「同步服务器」输入框已删除，`setServer()`/`restore()` 强制使用内置地址（忽略历史 meta）。改动未触碰 IPC 签名，main/preload 无需同批改。已重建 dist（renderer bundle index-Csv1mJRi.js）。
- **UI v2 现代化改版（2026-09-16）**：全 emoji 替换为线性 SVG 图标集（`components/icons.tsx`，lucide 风格自绘，零依赖）；styles.css 重写为现代深色设计系统（极光背景、卡片按计时器自身颜色做色晕+悬浮彩色描边+入场瀑布动画、运行中呼吸光晕、D-day 流光徽章、弹窗弹簧动画、分段式类型选择器、自定义 checkbox）。
- **同步契约 bug 修复（2026-09-16，重要）**：Java 版 push 响应漏 `operation_id` 字段（Node 开发版有）→ 客户端 sql.js 绑定 undefined 报错「Wrong API use: tried to bind...」，pending_ops 无法消费、每 4s 循环报错。修复 = 服务端 processOne 补 `result.put("operation_id", operationId)` + 客户端 pushPending 加空值防御（缺失跳过，下轮幂等收敛）。新 jar 已部署重启，生产回归 4/4（cloud-verify.mjs 支持 `VERIFY_BASE` 环境变量 + operation_id 断言），测试数据已清。
- **表单体验修复（2026-09-16）**：目标日期由原生 date input 换为年/月/日三列下拉（联动天数/闰年，杜绝原生弹窗样式不可控）；颜色增加自定义取色器（彩虹色板包裹 input[type=color]，非预设色自动显示为当前色）。
- **最新 exe = `release5/`**（含 UI v2 + 本轮全部修复）；release4 为旧版。
- **UI v3 手动分段 + 台钟视图 + 动效修复（2026-09-16，最新）**：
  - **手动分段**：新 IPC `timers:segment`（main/preload/global.d.ts 同批改，已重建 dist）。`engine.segment()`：精确倒计时运行中按「分段」= 写 SEGMENT 记录（旧 session）→ 立即按预设时长开启下一轮（新 session_id）；正计时 = 记一个 lap 继续累计。`run_json` 增加 `segments_ms[]`（随运行状态同步），DTO 暴露 `segmentsMs`；重置/到点自动清空。卡片与台钟均含「分段」按钮 +「已分 N 段」徽章。
  - **台钟全屏视图**：新组件 `components/ClockView.tsx`——320px 大环 + 环内大数字、本次分段列表、控制按钮与卡片一致，Esc/遮罩/关闭钮退出（260ms 退场后卸载）；卡片主体可点击打开（App 持 openId，tick 数据直通实时刷新）。
  - **卡片**：精确倒计时 idle 直接显示预设时长（替代「待开始」）+ 环形进度常驻（idle 满环降透明度），卡片布局不再随状态跳变。
  - **动画根因修复**：`.card.running` 原重定义 animation 列表，每次开始/暂停/结束都会重放入场动画（卡片跳动的元凶）→ 运行光晕移至 `.card::after` 独立伪元素；`card-in` fill 由 both 改 backwards（fill forwards 会永久覆盖 FLIP 的 inline transform）；网格重排新增 FLIP 动画（App 内 `useFlipGrid`，data-flip-key + 双 rAF，支持中断续播）；act-btn/chip 入场微动画；圆环 dashoffset 0.5s linear 与 500ms tick 节奏对齐（视觉匀速扫描无跳格）；ring-in 弹簧入场。
  - 渲染层 bundle `index-CnrH7yHt.js` / `index-BTFC-TIr.css`。**最新 exe 在 `release6/`**（release5 及更早为旧版）。新坑：PowerShell `*>` 重定向日志为 UTF-16，Read 工具视为二进制，需先 `Get-Content | Set-Content -Encoding utf8` 转码。
- **性能修复：动画卡顿与打点反馈延迟（2026-09-16，最新）**：
  - **根因 1（主因）：GPU 被无条件全局禁用**。`main.ts` 原为沙箱/无 GPU 环境加的 `disable-gpu + disable-gpu-compositing + disableHardwareAcceleration` 被打进了正式运行路径 → 整窗极光光斑漂移/呼吸光晕/SVG 环动画全部 CPU 光栅化，渲染进程长期高负载，点击反馈排队。已改为条件化：**真机默认硬件加速；`TIMEMARK_SOFTWARE_RENDER=1` 时才禁用**（沙箱逃生门保留）。
  - **根因 2：打点/暂停/继续反馈依赖下一个 500ms tick**。所有动作 IPC 返回的最新 DTO 原来被丢弃，UI 只能等 `applyTick`。已在 App.tsx 加 `mergeDto()`：动作返回值立即合并进列表 + 同步 `prevRun` 镜像防到点误判（start/pause/resume/reset/segment/end/stop/星标/置顶全覆盖）。
  - **根因 3：无效渲染负载**。主进程每 500ms 无条件广播全量 tick（空闲时数据没变也发）→ 加签名去重（`lastTickSig`，JSON.stringify(engine.list())，不变不发、checkDday 同步跳过）；同步每 4s 必发 syncing→idle 两个新对象 → App.tsx `applySync` 浅比较（state/pending/error/lastSyncAt 全等则不 setState）。
  - 用户实测环境为 dev 模式（`%APPDATA%\timemark-windows`，package.json name；非 exe 的 `%APPDATA%\TimeMark`），重建 dist 即生效；renderer bundle `index-zsOl_Y2m.js` / `index-C2hXQv2Q.css`。**最新 exe 在 `release12/`**（v0.2.1，性能修复版：`TimeMark Setup 0.2.1.exe` + `TimeMark-Portable-0.2.1.exe`；asar grep 验证 TIMEMARK_SOFTWARE_RENDER/lastTickSig/index-zsOl_Y2m 三标记全进包；release6–release11 均为旧版）。沙箱冒烟：主进程/DB/同步循环正常（沙箱渲染帧回收为本环境已知限制，真机不受影响）。
- **提前结束如实记录（2026-09-16，release13 = v0.2.2，最新）**：
  - 用户反馈：暂停状态下点「结束」弹「暂停状态下结束将不保存本次记录」并直接丢弃数据。原实现只支持运行中结束（segment+reset），暂停态无结算路径。
  - 修复：`engine.segment()` 支持暂停态——精确倒计时按 `preset - remaining_at_pause` 结算部分段、正计时按 `accumulated_ms` 结算 lap，如实写 SEGMENT 记录并透传 segments_ms（保持 paused 状态，继续后逻辑不变）；App.tsx「结束」对 running/paused 统一走 segment+reset，删除"不保存"确认框。正计时暂停结束本就经 stop() 如实记录，未动。
  - 历史页模型天然兼容：纯 SEGMENT 会话 total=段和。TS 坑：`const running = ... && !!rt.segStartMonoNs` 的别名条件不能为可变属性收窄类型 → 两处 `monoDeltaMs(rt.segStartMonoNs!)` 补非空断言（首次构建 EXIT=2 教训）。
  - asar 验证标记：`remaining_at_pause: run.remaining_at_pause`（引擎暂停分支唯一串）+ `index-L0PSG8SP`（新渲染层）均进包。**最新 exe = `release13/`**（release12 及更早为旧版）。
- **功能批次 v0.3.0（2026-09-16，release14 = 最新）**——用户 9 项反馈一次性落地：
  1. **今日专注/完成轮次不涨根因**：原逻辑只有番茄钟阶段"自然倒计时到 0"才写 journal，跳过/提前结束全丢弃。修复：pomoSkip 按实际进行时长计账（专注段 workCount+workMs、休息段 breakMs，>5s 防误触）。
  2. **历史筛选**：时间（今天/近7天/近30天/全部）+ 类型（番茄钟/倒计时/正计时/日期，按 timer 派生 kind）+ 颜色（色点圆钮）三行筛选。
  3. **记录删除**：新 IPC `records:delete` → engine.deleteRecords 软删除墓碑（version+1，随同步收敛多端）；详情页可删单段或整条会话。
  4. **分段重命名**：localStorage `tm-seg-names`（键 = timer_record.id，上限 30 字），详情页改名、展开列表同步显示；注意：本地存储不参与多端同步（未动同步契约）。
  5. **滚轮调数**：TimerForm 全部数字输入换 WheelNumber（原生非 passive wheel 监听 preventDefault，step=1，min/max 钳制）。
  6. **正计时纯秒显示**：formatSeconds(ms, withCs)；舞台大数字/台钟巨号/侧栏行值全改秒（.unit 复用既有样式）。
  7. **历史卡片收起/展开 + 二级详情**：默认收起，箭头钮展开分段明细；点卡片开 rec-detail 弹窗（改段名/删段/删整条/最终结算行）。
  8. **标签系统**：引擎 listTags/setTimerTags（tag 表按名去重复用、timer_tag 链接增删、色板按名哈希，上限 8 个/计时；数据走既有 tag/timer_tag 表**参与同步**，远程变更经 onRemoteChange 刷内存映射）；DTO 加 tags；表单标签编辑器（回车添加+datalist 联想）；侧栏标签筛选行；标题行 mini chips。新 IPC：tags:list / timers:setTags（main/preload/global.d.ts 同批改）。
  9. **窗控加大**：12px 点 → 30×28 圆角按钮 + hover 底色（含关闭键红色 hover）。
  - 验证：主进程 tsc EXIT=0；渲染层严格类型检查 EXIT=0（CLI 检查须显式带上 global.d.ts，否则 Window.timemark 全报错——工具坑）；asar 五标记命中（tm-seg-names/timers:setTags/records:delete/index-Bn-KQcWo/wheel）。**最新 exe = `release14/`**（release13 及更早为旧版）。
- **历史保留已删计时记录 + 置顶标识（2026-09-16，release15 = v0.3.1，最新）**：
  - 用户反馈：删除计时器后其历史记录连带消失；清单里置顶项无特殊标识不好区分。
  - 根因：历史页按"存活计时器"查名称/类型，查不到的记录整条跳过。
  - 修复：新 IPC `timers:metas` → engine.listMetas()（**含已删除**计时元的 name/color/type/config/deleted）；App 持 metas 状态（refreshRecords 一并刷新，删除计时后也刷新）；HistoryView 改用 metas 全量查找，已删除计时的记录保留并在卡片/详情标注「计时已删除」红色徽章；isPomodoro/getPomodoro 参数放宽为 Pick<TimerDTO,'type'|'config'> 以接受 TimerMeta。
  - 置顶标识：侧栏行名称后加琥珀色 IconPin（filled）。
  - asar 验证：timers:metas / listMetas / pin-mark / index-BUF45YTL 全命中。**最新 exe = `release15/`**（release14 及更早为旧版）。
- **台钟打点补齐（2026-09-16，release16 = v0.3.2，最新）**：
  - 用户反馈：双击进台钟后没有打点功能。根因：FullscreenClock 组件压根没接 onSegment 通道（v3 实现时未同步打点功能）。
  - 修复：FullscreenClock 加 onSegment prop，运行中（非番茄钟）显示「打点」按钮；新增已打点分段 chips 行（序号+时长，进行段琥珀高亮，复用 .clock-seg-chip 新样式）；提示文案补「N 打点」。
  - 顺手修键盘缺陷：台钟打开时 Space/N 原本作用于侧栏选中项 → 改为 fullscreenTimer ?? selected。
  - 验证：双 tsc EXIT=0；asar 标记 clock-seg-chip + index-De3tXbaA 命中。**最新 exe = `release16/`**（release15 及更早为旧版）。
- **历史页 UI 打磨（2026-09-16，release17 = v0.3.3，最新）**——用户 5 项反馈：
  1. 滚动条压内容：`.history` 加 `scrollbar-gutter: stable` + 右侧 8px 内边距 + 专属 6px 半透明细滚动条；同时根治"点筛选布局跳变"（滚动条出现/消失导致宽度抖动）。
  2. 颜色筛选对齐：色点 18px 改 20px、align-self:center、去掉 border+outline（激活改双层 box-shadow 环，零布局位移）。
  3. 自定义查看某天：新增「某天」chip → 展开年/月/日三列 Select（复用组件），buildEntries 加 until 上界；标题显示「N 月 N 日」；快捷范围 chips 保留。
  4. 展开按钮重做 + 展开动画：幽灵圆钮（无灰底方块），箭头收起朝下/展开朝上 0.28s 回弹旋转；分段区改 grid-template-rows 0fr→1fr 平滑展开 + 内容延迟淡入；卡片恢复 `--i` 入场瀑布（上批误写 0）+ hover 上浮 1px。
  5. 详情弹窗删掉「关闭」按钮（右上 × 保留），底栏只留「删除本次记录」。
  - 验证：双 tsc EXIT=0；asar 标记 rec-seg-wrap / scrollbar-gutter / index-CdjIWg19 命中。**最新 exe = `release17/`**（release16 及更早为旧版）。
- **窗控贴角加大（2026-09-16，release18 = v0.3.4，最新）**：按钮 30×28→36×32、间距 6→2px、`margin-right:-24px` 抵消主区右内边距贴向右上角，图标 15/12/15。asar 验证（注意 vite 压缩后 CSS 无空格，grep 要用压缩格式）。**最新 exe = `release18/`**（release17 及更早为旧版）。
- **详情弹窗居中 + 误触修复（2026-09-16，release19 = v0.3.5，最新）**：
  - 用户反馈：点卡片弹窗很慢/有时点不开、弹窗不居中。
  - 根因 1（不居中）：rec-detail 弹窗渲染在滚动容器 `.history` 内，该容器带 `animation: rise-in ... both`（transform 动画）→ Chromium 中成为 fixed 遮罩的包含块 → 遮罩只覆盖历史列，弹窗"相对历史列居中"。修复：**createPortal 到 document.body**（真正全窗口居中，且脱离滚动容器层级）。
  - 根因 2（点不开）：双击时第二击落在弹出的遮罩上立即关闭 = 感知"没打开"。修复：openedAtRef 记录打开时刻，遮罩 300ms 内点击不关闭；整卡可点（原来只有头部/汇总行可点，点进度条无反应也是"点不开"的一部分）。
  - 附加：详情打开时 Esc 关弹窗（capture 阶段 stopPropagation，防上层同时切回聚焦视图）。
  - 验证：双 tsc EXIT=0；asar createPortal + index-6eDNlKUV 命中。**最新 exe = `release19/`**（release18 及更早为旧版）。
- **新图标 + 检查更新 + GitHub 开源（2026-09-16，release20 = v0.4.0，最新）**：
  - **应用图标重绘**（`apps/windows/tools/gen-icon.cjs`）：深空圆角方底 + 青→紫渐变 270° 倒计时环 + 柔光 + 10:10 表针，与 UI v3 令牌一致；修了圆角判定左下角 Y 偏移笔误。产物 `assets/icon.png`（窗口/托盘）+ `build/icon.png`（exe 资源）。
  - **检查更新**：仓库 `apps/windows/latest.json` 为版本清单（jsDelivr CDN → raw.githubusercontent → ghproxy 三源回退，8s 超时）；启动 8s 后 + 每 6h 静默检查，发现新版发系统通知；侧栏同步菜单新增「检查更新」（当前 v{ver} / 检查中 / 已是最新 / 发现新版本 vX）+「前往下载新版本」（shell.openExternal releases 页）。未做静默自更新（需 electron-updater + 签名，后续可加）。发布流程：bump package.json + latest.json → commit push → 把 exe 挂到 GitHub Releases。
  - **GitHub 推送完成**：`git@github.com:zibi666/T-Minus.git` main 分支（131 文件首提交）。`.gitignore` 排除 node_modules/dist/release*/.workbuddy/backend/target/tools 临时文件与 ssh 密钥配置；推送前敏感扫描通过（DB 密码仅环境变量，无硬编码）。⚠️ 仓库含服务器 IP（118.195.133.25，客户端内置地址），若担心暴露可转私有。
  - 验证：双 tsc EXIT=0；asar 标记（repo/update:check/jsDelivr）全命中。**最新 exe = `release20/`**（release19 及更早为旧版）。
- **应用内品牌标识换新（2026-09-16，release21 = v0.4.1，最新）**：
  - 用户反馈 exe 图标换了但应用内侧栏品牌 Logo 还是旧橙色。根因：侧栏 Logo 是独立 SVG 组件 `LogoMark` + `.brand-logo` 的 `--accent-grad`（珊瑚橙）底，与 exe 图标是两套。
  - 修复：`.brand-logo` 底改深空渐变（#1B2130→#0B0E18 + 细描边）；`LogoMark` 重绘为轨道 + 270° 青紫渐变环（#4DC9F0→#9381FF，dasharray 70.7/23.6）+ 10:10 表针，与 exe 图标完全统一。CTA 按钮保留珊瑚橙 accent（动作色不受影响）。
  - 已提交推送 GitHub（86e6459）。验证：双 tsc EXIT=0；asar 标记 logoGrad/logoArc 命中。**最新 exe = `release21/`**（release20 及更早为旧版）。
- **更新入口显性化（2026-09-16，release23 = v0.4.2，最新）**：
  - 用户反馈找不到手动检查更新。根因：入口藏在侧栏同步行的弹出菜单里，而**登录态点击同步行只触发"立即同步"、从不弹菜单**——登录用户永远看不到菜单。
  - 修复：同步行点击一律弹出菜单（立即同步已在菜单内）；行尾新增**齿轮按钮**显性入口（有新版时琥珀色高亮）；title 更新为「设置与更新」。icons.tsx 新增 IconGear（lucide settings）。
  - 打包注意：用户实例运行中会挡同输出目录，**换新目录（release23）打包不受影响**。已推送 GitHub（latest.json 同步 0.4.2）。验证：双 tsc EXIT=0；asar gear/设置与更新 命中。**最新 exe = `release23/`**（release22 空目录废弃，release21 及更早为旧版）。
- **三处 UI 微调（2026-09-16，release24 = v0.4.3，最新）**：①窗控按钮组 `align-self:flex-start + margin-top:-4px` 顶到标题行上缘；②历史页滚动条贴右缘：`.history` 负右外边距 -30px 扩到窗口缘附近 + 内边距 34px 补偿内容宽度；③分段占比条 3 色轮替扩为 8 色（青/紫/薄荷/琥珀/珊瑚/蓝/粉/青柠，BAR_COLORS）。验证：双 tsc EXIT=0；asar 三标记（align-self:flex-start/#F472B6/margin-right:-30px）命中。**最新 exe = `release24/`**（release23 及更早为旧版）。
- **release 目录大清理（2026-09-16）**：22 个旧 release（原 12.3GB）已清空 95%——只剩每个目录里一个被 WorkBuddy 宿主进程锁住的 `app.asar` 残壳（约 28MB/个）。沙箱内所有删除通道（Remove-Item/.NET/python）都被 `[safe-delete]` 劫持到回收站且对 app.asar 失败（WinError 32 句柄占用，非沙箱可解）。用户已自行执行 cleanup bat：残壳清掉、release24 改名为 `release` 并从其中运行 0.4.3 便携版。此后打包约定：唯一固定输出目录 `apps/windows/release/`，重打包前先清空（应用运行中则换新目录再打）。
- **「完成一次专注两数字不动」终查（2026-09-16，release25 = v0.4.4，最新）**：
  - **DB 实证**（sqlite 直查 `%APPDATA%/timemark-windows/timemark.db`）：用户 21:02:29 完成"1"（10s 普通倒计时）的 PRECISE 记录**已正常落库**，今日共 7 次完整计时 + 19 段打点，合计专注约 8:51——引擎侧一直正常。
  - **进程实证**（WMI）：截图时刻跑的是旧便携版实例；用户后来已自行启动 `release\TimeMark-Portable-0.4.3.exe`（cleanup bat 已被用户执行：残壳已清、release24→release）。旧实例的 records 快照冻结导致面板 0:08 假象。
  - **真实设计缺口修复**：完成轮次此前只统计番茄钟 journal（用户计时器全是普通倒计时/正计时 → 永远 0）。修复：stats 中非番茄钟 final 记录（PRECISE/STOPWATCH）每条 +1 轮，rounds = journal.workCount + extraRounds。
  - 注意：esbuild 会混淆局部变量名，asar grep 验证局部变量（如 extraRounds）不可行——用 release24/25 asar SHA256 差异验证（same=False）。
  - **最新 exe = `release25/`**（v0.4.4）；cleanup-releases.bat 已更新为保留 release25。
- **单一 release 目录落实（2026-09-16 21:30，用户第二次强调后）**：
  - 用户批评正确：v0.4.4 不该再开 release25。已整改：v0.4.4 的 Setup/Portable/blockmap 全部并入 **`apps/windows/release/`**（唯一目录，package.json 默认输出即此），win-unpacked 也已换为 0.4.4（asar SHA256 EFA98D925247 验证）。
  - 过程坑（记录给后续会话）：①沙箱 `[safe-delete]` 垫片会劫持**所有子进程**（含 python）的删除，目录级/多文件回收必失败；②**WorkBuddy 宿主对 .asar 文件有独占句柄**（它解析 asar），会话内 asar 删/改/截断全被拒（WinError 32）——本会话创建的 asar 也一样；③对策：打包一律输出到全新目录 → Copy-Item 产物进 `release/` → python 逐文件清空临时目录（非 asar 文件都能删）→ asar 残壳留待 WorkBuddy 重启后删。
  - 当前残留（共 3 个文件 + 1 个旧便携 exe，均被句柄锁定）：`rb_tmp/`、`release25/` 两个壳目录（各剩 1-2 个 asar，约 84MB）+ `release/TimeMark-Portable-0.4.3.exe`（运行中实例本体）。**用户关闭 TimeMark 并重启 WorkBuddy 后，双击 `tools/finish-cleanup.bat` 即归一无痕**。
  - **今后铁律：打包只输出到 `apps/windows/release/`；TimeMark 运行中则先关再打，或临时目录+拷贝+清空，绝不新建 releaseN。**
- **M2 前置完成：TIMEMARK_DATA_DIR 数据目录开关（2026-09-16 22:15，release = v0.4.5，最新）**：
  - main.ts 顶部（ready 前）读 `TIMEMARK_DATA_DIR`：mkdir + `app.setPath('userData'/'sessionData')` → 数据库、localStorage（journal）、会话数据全隔离；IPC `app:data-dir` 返回 {dir, overridden}；同步菜单底部新增数据目录信息行（env 模式青色高亮，方便区分双实例）。
  - **双实例对测用法**：实例 A 默认启动并登录账号；实例 B（PowerShell）：`$env:TIMEMARK_DATA_DIR='E:\T-Minus\apps\windows\data-b'` 后再启动便携版 → 全新数据目录 → 登录同账号 → 自动 pull。对测清单：离线修改→恢复网络收敛；一端删除→另一端墓碑同步；同字段两端改→LWW 收敛；强杀一端→本地不丢。
  - 打包按铁律直出 `release/`（TimeMark 未运行，一次成功）；0.4.4 旧产物已清，目录内仅剩 0.4.5 三个产物 + win-unpacked。
  - 验证：双 tsc EXIT=0；asar 哈希 8C1D2960EBC0（区别于 0.4.4 的 EFA98D92）。
  - **本地已提交（e908dbc）未推送**——本次 git push 触发 SSH 密钥访问授权被用户拒绝，遵循不重试原则；需要推送时用户自行 `git push` 或明确授权后再推。**最新 exe = `release/`（v0.4.5）**。
- **Android 端 M3.0–M3.4 一次性落地（2026-09-17 00:25–00:35，用户令"明早要看到 app"）**：
  - **本机工具链从零搭建**：JDK 17.0.2（华为云镜像）+ Gradle 8.10.2（腾讯云镜像）+ Android cmdline-tools（dl.google.com）→ `tools/android/`；sdkmanager 接受许可后装 platform-35 + build-tools 35.0.0 + platform-tools（license 管道：100 行 'y' 文件管道输入才生效）。
  - **完整工程 `apps/android`**（21 个源文件，Kotlin 2.0.20 + AGP 8.7.1 + Compose BOM 2024.09.03 + Room 2.6.1 KSP + Retrofit 2.11 + kotlinx.serialization；阿里云镜像仓库）：四类型计时引擎（elapsedRealtime 墙钟/单调守卫/跨重启恢复）、前台服务（specialUse FGS）+ 精确闹钟检测与降级（setExactAndAllowWhileIdle/setAndAllowWhileIdle）+ BOOT 恢复、Room 全表（含 pending_ops/sync_cursor DataStore）、同步客户端（register/login/push/pull、operation_id 幂等、dirty 规则、游标整页推进、登录认领孤儿数据并入队上传——比 Windows 更完整）、UI 五屏（清单+统计概览/台钟详情/表单/历史/登录）、TIMEMARK_DATA_DIR 数据目录开关同步实现、v3 品牌视觉。
  - 明确裁剪（留 M3.5+）：标签 UI、Glance 小组件、番茄钟长休息逻辑（与 Windows 保持一致：break 固定 short_break_ms）、通知栏操作按钮。
  - **构建三轮迭代成功**（首轮 20 处编译错：实体缺 @Serializable、导入缺失、局部变量混淆名等；最终 `BUILD SUCCESSFUL`）。**APK：`apps/windows/release/TimeMark-0.4.5-android.apk`（17.5MB debug 签名，可直接安装）**。
  - 用户侧待办：手机安装（允许未知来源）→ 注册/登录 → 与 Windows 端同账号互测同步。
  - **追加**：引擎单测 9/9 全绿（date_days_left 首版差 1 天 → 按 Windows dateLogic.ts 对齐修正：days = 目标 − 今天，includeToday 时 +1）；APK 已刷新至 `release/TimeMark-0.4.5-android.apk`；本地提交 cae8af9（含 e908dbc/f0a2b86 共 3 commits 领先 origin，推送待用户授权）。
- **Android 端三缺陷修复（2026-09-17 上午，v0.4.6 = 最新）**——用户真机反馈：登录 401 / 创建时钟闪退 / 图标白边，全部根因定位 + 模拟器实证修复：
  1. **登录必 401**：`SyncManager.register/login` 收到 username/password 参数后弃用，`authFlow` 实际发送 `pendingUsername/pendingPassword` 字段（从未被赋值，恒空串）→ 服务端必返 401。修复 = authFlow 显式接收 AuthReq。模拟器对生产服务器实测：probe_user_x9 登录成功显示"已登录"。
  2. **创建时钟必闪退**：`Common.kt` 的 Chip/TypeBadge 写了 `RoundedCornerShape(999)`——**Int 重载是百分比（合法 0–100）**，999% 直接 `IllegalArgumentException: The percent should be in the range of [0, 100]`。列表页空清单不渲染这两个组件所以不崩；点「+」进表单、类型选择 chips 一组合即崩（历史页范围筛选 chips 同雷）。修复 = `RoundedCornerShape(50)`。模拟器全流程实证：表单打开→输入→保存→列表卡片，零崩溃。
  3. **图标白边**：launcher icon 是普通 vector（非自适应图标），启动器垫白色圆底。修复 = 新增 `mipmap-anydpi-v26/ic_launcher.xml`（adaptive-icon：`@color/ic_launcher_bg` 深空底 + `drawable/ic_launcher_fg` 前景环 + monochrome）；manifest 改 `@mipmap/ic_launcher`；通知栏小图标换 `ic_notification`（单色环，原来用彩色 launcher 图标会糊成方块）。抽屉截图验证无白边。
  - 顺手修：调色板 hex 生成 `cc.red.toInt()*255`（Float 先截断恒 0 → 所有色存成 #000000）→ `(red*255).toInt()`（FormScreen 1 处 + HistoryScreen 2 处）。
  - **根因抓取方法（复用价值高）**：本机 WHPX 模拟器复现真机崩溃——sdkmanager 装 emulator + `system-images;android-35;default;x86_64`，`avdmanager create avd -n tmtest`，headless 启动（-no-window -gpu swiftshader_indirect），adb 驱动 UI（uiautomator dump 找坐标 → input tap/text），`logcat -d -b crash` 拿堆栈。注意：edge-to-edge 下 AOSP 键盘悬浮遮挡表单下半部，input tap 前先 `input keyevent 4` 收键盘。
  - 版本 0.4.6 / versionCode 2。**最新 APK = `apps/windows/release/TimeMark-0.4.6-android.apk`**（0.4.5 已删，真机直接覆盖安装）。本地提交未推送。
  - 遗留测试数据：生产库 probe_user_x9 + 名下 SmokeTest 计时器（登录验证产物，可择机用 DB 脚本清理）。

## 二、关键决策

- **端口**：8080 让给 sub2api，**TimeMark 后端用 18080**（客户端登录框填 `http://118.195.133.25:18080`）。
- **不停服务器上任何现有服务**；内存足够共存。
- **安全提醒**：云数据库 root 密码在对话中出现过 → 部署跑通后在腾讯云控制台改密，改完只需更新服务器上的 `TIMEMARK_DB_PASSWORD`。

## 三、待办（按优先级）

1. ~~部署正式后端到服务器~~ **已完成（2026-09-16），见「已完成 §6」**
   - 遗留收尾：**改云数据库 root 密码**（密码在对话中再次出现过）→ 腾讯云控制台改密后，服务器上 `sudo systemctl edit timemark` 更新 `TIMEMARK_DB_PASSWORD` 并重启服务。
   - 客户端验收：接口地址已内置，直接打开客户端注册一个真实账号走通注册/同步（API 级注册冒烟因审批拦截未做，待客户端验证）。
2. **M2 双实例同步验收**：给客户端加数据目录开关（env 如 `TIMEMARK_DATA_DIR`），两个 Windows 实例同账号对测 §8 M2 清单（离线修改、幂等、墓碑、冲突、宕机不影响本地）。
3. **M0 遗留（时效性最强）**：鸿蒙实况窗 TIMER 场景 AGC 权益申请（审核数个工作日，越早越好）；Android 14 精确闹钟真机验证；OpenAPI 契约文档。
4. **Android / 鸿蒙端开发**（M3/M4，依赖 1+3）。
5. ~~Windows 打包 exe~~ **已完成（2026-09-16）**：electron-builder 26（devDependencies），配置在 package.json `build` 字段；产物 `apps/windows/release3/`：`TimeMark Setup 0.1.0.exe`（NSIS 安装版）+ `TimeMark-Portable-0.1.0.exe`（便携版，双击即用），各约 95MB，含自绘图标（`tools/gen-icon.cjs` 生成 → `assets/icon.png` 运行时窗口/托盘 + `build/icon.png` exe 资源）。重打包命令：`npx electron-builder --win`（走 npmmirror 镜像环境变量）。注意：exe 未签名（无证书），首次运行可能有 SmartScreen 提示；electron-builder 会复用输出目录，若遇 `EBUSY` 换 `--config.directories.output=<新目录>` 即可。

## 四、踩坑备忘（避免重复踩）

- WorkBuddy 环境：shell 输出捕获不稳 → 结果写文件再 Read；`ELECTRON_RUN_AS_NODE=1` 全局存在 → 启动 Electron 前必须清。
- Electron：无 GPU/受限环境用 `--in-process-gpu` + `--disable-gpu-sandbox`（仅 disableHardwareAcceleration 不够）；preload.ts 与 IPC 面必须同批更新。
- sql.js：多语句 DDL 逐条执行；`value` 是 H2 保留字（meta 表列名用 val）。
- Spring Boot 打包时 jar 被运行中进程锁住 → Windows 重建前先停 java。
- 客户端 `__run` 标记在 **row 内**（不是 op 顶层），服务端运行状态校验据此判断。
- systemd/环境变量里的 `SERVER_PORT` 会被宿主环境污染 → 用专属名 `TIMEMARK_SERVER_PORT`。
